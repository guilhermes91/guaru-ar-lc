import { notFound } from "next/navigation"; import type { Metadata } from "next"; import { CheckCircle2, MessageCircle } from "lucide-react"; import { services, site } from "@/data/site";
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const s=services.find(x=>x.slug===slug);return s?{title:`${s.title} no Guarujá`,description:`${s.description} Atendimento Guaru Ar LC no Guarujá e região.`}:{}}
export default async function Service({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();return <><section className="page-hero"><div className="container"><div className="breadcrumbs">Início / Serviços / {s.title}</div><h1>{s.title} no Guarujá</h1><p>{s.description}</p><a className="btn whatsapp" href={site.whatsapp(`Olá! Preciso de orçamento para ${s.title}.`)} target="_blank"><MessageCircle/> Solicitar orçamento</a></div></section><section className="section"><article className="container content"><h2>Atendimento especializado</h2><p>A Guaru Ar LC oferece avaliação cuidadosa, orientação transparente e execução profissional no Guarujá. Nossa equipe identifica a necessidade do equipamento e recomenda a solução mais adequada.</p><h2>O que está incluído</h2><ul><li><CheckCircle2 size={17}/> Avaliação técnica e diagnóstico</li><li><CheckCircle2 size={17}/> Orçamento claro antes da execução</li><li><CheckCircle2 size={17}/> Serviço organizado e cuidado com o ambiente</li><li><CheckCircle2 size={17}/> Orientações de uso e conservação</li></ul><h2>Atendimento residencial, comercial e condominial</h2><p>Atendemos casas, apartamentos, condomínios, comércios, pousadas e empresas em diferentes bairros do Guarujá.</p></article></section></>}
